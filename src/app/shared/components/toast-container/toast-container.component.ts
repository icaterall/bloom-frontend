import { Component } from '@angular/core';

/**
 * Legacy toast container — replaced by ngx-toastr.
 * Kept as empty stub to avoid breaking any remaining references.
 */
@Component({
  selector: 'app-toast-container',
  standalone: true,
  template: '',
})
export class ToastContainerComponent {}
